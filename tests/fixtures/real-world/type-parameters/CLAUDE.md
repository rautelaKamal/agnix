# API Reference

## Validators

Use validator types like <T> for generic parameters.
The <Option> type wraps nullable values.
Map keys use <Key> and values use <Value>.
The <TableName> type references database tables.
