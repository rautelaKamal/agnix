{
  "name": "file-reader",
  "description": "Reads file contents from the filesystem and returns the text",
  "inputSchema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "The path to the file to read"
      }
    },
    "required": ["path"]
  },
  "requiresApproval": true
}
